import React from 'react';
import { Text } from 'react-native';

const styles = {
  container: {}
};

export default class extends React.Component {
  render() {
    return <Text style={styles.container}>setting</Text>;
  }
}
