import React from 'react';
import { Text } from 'react-native';

const styles = {
  container: {
    marginTop: 20
  }
};

export default class extends React.Component {
  render() {
    return <Text style={styles.container}>homeDetail</Text>;
  }
}
